apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-tower
spec:
  capacity:
    storage: 8Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server:   {{ p_server }}
    path:     {{ p_path }}/postgres-tower
    readOnly: false
