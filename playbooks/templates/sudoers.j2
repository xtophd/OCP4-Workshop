## We need /usr/local/bin in root's path for 'sudo -i'

Defaults    secure_path = /sbin:/bin:/usr/sbin:/usr/local/bin

## Allow student to run any commands anywhere

{{ g_studentName }} ALL=(ALL) NOPASSWD: ALL
