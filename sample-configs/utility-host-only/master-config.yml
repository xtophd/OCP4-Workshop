##
##    !! WARNING !!  Ansible only interprets 'True' and 'False'
##                   as booleans when reading ini files.  Other
##                   values such as 'true' or 'false' would be
##                   interpreted as strings and would break various
##                   logic tests.
##
##                   When reading YML files, booleans can take on
##                   many forms and be interpreted correctly.
##
##                   For consistency, only use unquoted booleans: True & False

all:

  vars:

    ## ----------------------------------------
    ##    Globals that will be phased out

    g_clusterAdmin:       "cloud-admin"
    g_clusterUser:        "cloud-user"


    ## ----------------------------------------
    ##    Workshop Variables

    workshop_vars:

      clustername:          "ocp"

      ocp_version:          "4.9"
      ocp_dns_wildcard:     "apps"

      network_basedomain:   "example.com"
      network_network:      "192.168.1.0"
      network_netmask:      "255.255.255.0"
      network_broadcast:    "192.168.1.255"
      network_prefix:       "24"
      network_gateway:      "192.168.1.1"
      network_nameserver:   "192.168.1.30"
      network_timeserver:   "192.168.1.30"

      http_proxy:           ""
      https_proxy:          ""

      bastion:

        firewalld:          True
        dns:                True
        dhcp:               True
        dhcp_pxe:           True
        dhcp_uefi_http:     True
        haproxy:            True
        matchbox:           True
        nfs:                True
        ntp:                True
        squid:              True
        workshop_user:      True
        workshop_materials: True
        desktop_gui:        True
        desktop_chrome:     True
        desktop_vnc:        True

    workshop_extras:

      network_fqdn:         "{{ workshop_vars.clustername }}.{{ workshop_vars.network_basedomain }}"

      ocp_creds_dir:        "/root/ocp-{{ workshop_vars.clustername }}"
      ocp_wfqdn:            "{{ workshop_vars.ocp_dns_wildcard }}.{{ workshop_vars.clustername }}.{{ workshop_vars.network_basedomain }}"

      nfs_export:           "/home/nfs-{{ workshop_vars.clustername }}"

      squid_http_url:       "http://bastion.{{ workshop_vars.clustername }}.{{ workshop_vars.network_basedomain }}:3128"
      squid_https_url:      ""



    ## ----------------------------------------
    ##    Xtoph Deploy Variables
   

 
    ## THIS SAMPLE CONFIG DOES NOT USE XTOPH_DEPLOY,
    ## SO THESE PARAMETERS ARE MUTE

    #xtoph_vars:
    #
    #  deployhost_ip:       "192.168.1.170"
    #  clustername:         "{{ workshop_vars.clustername }}"



## ----------------------------------------
## HOSTGROUPS & VARS
## ----------------------------------------



## THIS SAMPLE CONFIG DOES NOT HAVE A DEDICATED 
## DEPLOYHOST, SO UNLIKE OTHER SAMPLE CONFIGS THAT
## USE XTOPH_DEPLOY, THESE PARAMETERS ARE MUTE

#myDeployhost:
#  hosts:
#    bastion:
#      h_pubIP: "192.168.1.150"
#  vars:
#    ansible_connection: local
#    ansible_host: "{{ inventory_hostname }}.{{ workshop_extras.network_fqdn }}"



## -----------------



myBastion:
  hosts:
    bastion:
      h_pubIP: "192.168.1.150"
  vars:
    ansible_connection: local
    ansible_host: "{{ inventory_hostname }}.{{ workshop_extras.network_fqdn }}"



## -----------------



myBootstrap:
  hosts:
    node1:
      h_pubIP:       "192.168.1.151"
  vars:
    ansible_host: "{{ inventory_hostname }}.{{ workshop_extras.network_fqdn }}"
    h_rhcosDEV:      "vda"
    h_rhcosNIC:      "dhcp"
    h_rhcosUSBdelay: "0"
    h_rhcosLOADER:   "bios"



## -----------------



myMasters:
  hosts:
    node2:
      h_pubIP:       "192.168.1.152"
      h_etcdName:    "etcd-0"
    node3:
      h_pubIP:       "192.168.1.153"
      h_etcdName:    "etcd-1"
    node4:
      h_pubIP:       "192.168.1.154"
      h_etcdName:    "etcd-2"
  vars:
    ansible_host: "{{ inventory_hostname }}.{{ workshop_extras.network_fqdn }}"
    h_rhcosDEV:      "vda"
    h_rhcosNIC:      "dhcp"
    h_rhcosUSBdelay: "0"
    h_rhcosLOADER:   "bios"



## -----------------



myWorkers:
  hosts:
    node5:
      h_pubIP:       "192.168.1.155"
    node6:
      h_pubIP:       "192.168.1.156"
  vars:
    ansible_host: "{{ inventory_hostname }}.{{ workshop_extras.network_fqdn }}"
    h_rhcosDEV:      "vda"
    h_rhcosNIC:      "dhcp"
    h_rhcosUSBdelay: "0"
    h_rhcosLOADER:   "bios"



